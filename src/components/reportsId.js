import React from 'react';
import Navigation from './navigation';

const Page = () => (
    <div>
        <h1 style={{color: 'yellow'}}>Reports ID</h1>

        <Navigation />
    </div>
);

export default Page;